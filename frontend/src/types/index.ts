export interface Category {
    id: number
    name: string
    color: string
}

export interface CategoryEntry {
    // category-id
    id: number
    name: string
    color: string
    time: number
}